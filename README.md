# Підключення SASS на localhost

1. Install Node JS https://nodejs.org/uk/
2. Instal SASS (npm install -g sass)
3. Відкрити папку scss.
4. Відкрити консоль.
5. Прописати sass --watch main.scss:main.css
